name: CRP Red Team Validator

on:
  push:
    paths: ['whitepapers/**.md']
  pull_request:
    paths: ['whitepapers/**.md']

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install markdown-it-py pymarkdownlnt requests

      - name: Run CRP Red Team Validator
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: |
          python crp_redteam_validator.py whitepapers/*.md > redteam_safety_report.md

      - name: Upload Report Artifact
        uses: actions/upload-artifact@v4
        with:
          name: redteam-safety-report
          path: redteam_safety_report.md

      - name: Comment on PR
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const report = fs.readFileSync('redteam_safety_report.md', 'utf-8');
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: `CRP Red Team Report:\n\n${report.slice(0, 2000)}... (Full report in artifact)`
            });